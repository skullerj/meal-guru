-- Drop recipe instructions support
-- Remove the recipe_instructions table and related indexes

-- Drop the table (this will also drop the indexes automatically)
DROP TABLE IF EXISTS recipe_instructions;