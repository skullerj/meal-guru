# Component Pattern Ledger

Tracks repeated UI patterns across the codebase. A pattern is promoted to a shared component only after appearing in 3 distinct places. Maintained by the `component-librarian` agent.

**Status values:** `tracking` (< 3 occurrences) | `ready-to-promote` (≥ 3 occurrences)

---

_No patterns tracked yet._
